# Multi-Agent Reinforcement Learning for Smart Traffic Control — Requirements
# =========================================================================
# Install dependencies with: pip install -r requirements.txt

# ── Core RL Environment ──────────────────────────────────────────────────
gymnasium>=0.29.0               # Gymnasium environment framework

# ── Deep Learning ────────────────────────────────────────────────────────
torch>=2.0.0                    # PyTorch deep learning framework

# ── Data Science & Calculations ──────────────────────────────────────────
numpy>=1.24.0                   # Array operations and numerical utilities
pandas>=1.5.0                   # CSV parsing, DataFrame metrics storage

# ── Visualization & Plotting ─────────────────────────────────────────────
matplotlib>=3.7.0               # High-quality data plots and figures

# ── Configuration & Logging ──────────────────────────────────────────────
pyyaml>=6.0                     # YAML loading for configuration parameters
tensorboard>=2.13.0             # Training logging and visualization interface
